# URL base de la API. Déjelo vacío para usar el mismo origen (producción con Express sirviendo el cliente)
# o el proxy de Vite en desarrollo. Ejemplo para un backend separado: https://api.midominio.com
VITE_API_URL=
